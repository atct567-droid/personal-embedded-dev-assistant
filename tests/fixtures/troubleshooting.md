# 故障排查

遇到 UART timeout 时，先确认 TX/RX 交叉、GND 共地和两端波特率一致，再检查 USART 时钟、GPIO 复用和接收中断是否打开。

## 看门狗复位

如果日志出现 watchdog reset，先读取复位原因寄存器，再检查任务是否持续喂狗、是否存在阻塞等待，最后缩小到最近一次改动。

## 数据链路

传感器采集完成后才允许生成结构化数据；发送失败时记录错误码和重试次数，不应把发送请求进入队列当作服务器已收到。

