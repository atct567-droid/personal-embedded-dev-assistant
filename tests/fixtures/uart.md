# UART 初始化

本演示板使用 USART1。PA9 配置为 TX，PA10 配置为 RX，空闲电平为高。
初始化顺序是打开 GPIO 和 USART 时钟、配置复用功能、设置 115200 波特率、8 个数据位、1 个停止位且无校验，最后使能发送和接收。

## 串口连接

调试线需要 TX、RX 和 GND 三根连接。发送端 TX 应连接到对端 RX，接收端 RX 应连接到对端 TX。

