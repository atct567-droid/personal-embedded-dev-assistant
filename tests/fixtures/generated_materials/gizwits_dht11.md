# 机智云示例中的 DHT11 采集（合成测试摘要）

## DHT11 时序

DHT11 读取前先把数据线配置为输出并拉低约 20ms，再拉高并等待约 30us，然后切换为输入并等待传感器响应。读取每一位时，先等待低电平结束，再等待高电平，根据约 40us 时刻的电平判断当前位是 0 还是 1。

## 数据和校验

一次读取包含 5 个字节，即 40 位数据。前两个字节表示湿度，第三、第四个字节表示温度，第五个字节是校验和。示例通过前四个字节相加并与第五个字节比较来判断数据是否有效；读取失败返回失败状态。
